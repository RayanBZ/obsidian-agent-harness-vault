# Standard engineering implementation task
agent-vault --vault $Vault dev "Implement robust folder ingest with file size limits and clear fail-point outputs" --agent-depth standard

# Deep engineering plan
agent-vault --vault $Vault dev "Design the next version of the CLI with GitHub clone support, PDF parsing, and image metadata ingest" --agent-depth deep

# Backend-focused task
agent-vault --vault $Vault dev "Implement safe zip extraction with path traversal protection and manifest generation" --agent-depth deep

# Frontend/dashboard task
agent-vault --vault $Vault dev "Create a React dashboard view for sessions, routes, agent depth, and recent outputs" --agent-depth standard

# Dry-run a coding task
agent-vault --vault $Vault dev "Refactor the router to support configurable routing from YAML instead of keyword matching" --agent-depth deep --dry-run
